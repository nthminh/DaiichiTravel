rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Cho phép đọc, ghi với mọi request
      allow read, write: if true;
    }
  }
}
